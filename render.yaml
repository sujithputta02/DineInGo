services:
  - type: web
    name: dineingo-backend
    env: node
    rootDir: backend
    buildCommand: bun install && bun run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production








